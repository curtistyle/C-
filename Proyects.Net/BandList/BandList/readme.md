### Atributo `ValidateAntiForgeryToken`

Especifica que la clase o metodo que este atributo es aplicado valida el anti-forgery token (o token anti-falsificacion).
Si el token anti-falsificacion no esta disponible, o si el token es invalido, la validacion fallara y el metodo de acción no se ejecutara.

### 

